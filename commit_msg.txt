Add marketing landing page at root route

- New LandingPage component: hero, feature cards, tier comparison, footer
- Locale-aware with full EN + AR translations
- [locale]/page.tsx: checks auth server-side, shows landing for guests, redirects to dashboard for users
- Root / still redirects to /en, which now shows landing page instead of forcing login
- Uses existing design system: ceramic surface, custom icons, gold shimmer, serif headers
- No auth-based redirects for unauthenticated visitors
